export interface productOptionsModel {
  product_options_id?: number;
  product_options_name?: string;
  active?: boolean;
  color?: boolean;
  size?: boolean;
  storage?: boolean;
  devices?: boolean;
}
